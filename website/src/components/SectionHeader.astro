---
interface Props {
  label?: string;
  title: string;
  subtitle?: string;
  align?: 'left' | 'center';
  light?: boolean;
}

const { label, title, subtitle, align = 'left', light = false } = Astro.props;
---

<header class={`section-header section-header--${align} ${light ? 'section-header--light' : ''} animate-on-scroll`}>
  {label && <span class="section-label">{label}</span>}
  <h2 class="section-title">{title}</h2>
  <span class="section-line" aria-hidden="true"></span>
  {subtitle && <p class="section-subtitle">{subtitle}</p>}
</header>

<style>
  .section-header {
    margin-bottom: var(--space-12);
  }

  .section-header--center {
    text-align: center;
    margin-left: auto;
    margin-right: auto;
    max-width: 720px;
  }

  .section-header--center .section-subtitle {
    margin-left: auto;
    margin-right: auto;
  }

  .section-label {
    display: block;
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: var(--space-4);
  }

  .section-title {
    font-size: clamp(1.75rem, 4vw, 2.75rem);
    font-weight: 500;
    letter-spacing: -0.02em;
    margin-bottom: var(--space-4);
  }

  .section-line {
    display: block;
    width: 80px;
    height: 1px;
    background: var(--color-accent);
    margin-bottom: var(--space-4);
    opacity: 0.5;
    transition: width 1s var(--ease-out-expo);
  }

  .section-header--center .section-line {
    margin-left: auto;
    margin-right: auto;
  }

  .js-enabled .section-line {
    width: 0;
  }

  .js-enabled .section-header.is-visible .section-line {
    width: 80px;
  }

  .section-subtitle {
    font-size: 1.0625rem;
    line-height: 1.7;
    color: var(--color-text-secondary);
    max-width: 560px;
  }

  .section-header--light .section-subtitle {
    color: var(--color-light-text-secondary);
  }

  .section-header--light .section-title {
    color: var(--color-light-text);
  }
</style>
